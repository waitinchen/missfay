# 🚀 Phi 系统快速启动指南（解决编码问题）

## ⚠️ 问题说明

PowerShell 在处理中文文件名时可能出现编码问题。已创建英文名称的替代脚本。

---

## ✅ 使用英文名称脚本（推荐）

### 窗口 A - 启动 GPT-SoVITS API

```powershell
.\start_gpt_sovits.ps1
```

**或手动执行**:
```powershell
cd "C:\Users\waiti\missfay\GPT-SoVITS-v3lora-20250228\GPT-SoVITS-v3lora-20250228"
.\runtime\python.exe api_v2.py
```

### 窗口 B - 启动 Voice Bridge

```powershell
.\start_voice_bridge.ps1
```

**或手动执行**:
```powershell
cd C:\Users\waiti\missfay
$py = ".\GPT-SoVITS-v3lora-20250228\GPT-SoVITS-v3lora-20250228\runtime\python.exe"
$env:GPT_SOVITS_URL = "http://127.0.0.1:9880"
$env:GPT_SOVITS_API_VERSION = "v2"
& $py voice_bridge.py
```

### 执行 QA 测试

```powershell
.\run_all_tests.ps1
```

**或直接运行**:
```powershell
.\qa_pressure_test.ps1
```

---

## 📋 脚本对照表

| 中文名称（可能编码问题） | 英文名称（推荐使用） |
|----------------------|-------------------|
| `启动Phi系统.ps1` | `start_voice_bridge.ps1` |
| `执行觉醒测试.ps1` | `run_awakening_test.ps1` |
| `全系统启动.ps1` | `start_all.ps1` |

---

## 🎯 完整启动流程

### 步骤 1: 启动 GPT-SoVITS API

```powershell
.\start_gpt_sovits.ps1
```

等待看到: `Uvicorn running on http://127.0.0.1:9880`

### 步骤 2: 启动 Voice Bridge

```powershell
.\start_voice_bridge.ps1
```

等待看到: `Service URL: http://0.0.0.0:8000`

### 步骤 3: 执行测试

```powershell
.\run_all_tests.ps1
```

---

## ✅ 所有可用脚本

**启动脚本**:
- `start_gpt_sovits.ps1` - 启动 GPT-SoVITS API
- `start_voice_bridge.ps1` - 启动 Voice Bridge

**测试脚本**:
- `qa_pressure_test.ps1` - 完整 QA 测试
- `run_all_tests.ps1` - 运行所有测试
- `check_services.ps1` - 检查服务状态
- `first_voice_test.py` - 首次语音生成测试

**工具脚本**:
- `fix_and_retest.ps1` - 自动修复和重试
- `verify_api.py` - API Key 验证

---

**使用英文名称脚本可避免编码问题！** ✅




