[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "python311Packages.virtualenv"]

[phases.install]
cmds = [
    "python3 -m venv venv && source venv/bin/activate && pip install -r requirements_phi.txt"
]

[phases.build]
cmds = []

[start]
cmd = "venv/bin/python -m uvicorn voice_bridge:app --host 0.0.0.0 --port $PORT"


