[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements_phi.txt"]

[phases.build]
cmds = []

[start]
cmd = "uvicorn voice_bridge:app --host 0.0.0.0 --port $PORT"


