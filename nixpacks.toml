[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "gcc"]

[phases.install]
cmds = [
    "pip install --break-system-packages -r requirements_phi.txt"
]

[phases.build]
cmds = []

[start]
cmd = "export LD_LIBRARY_PATH=/nix/store/*/lib:$LD_LIBRARY_PATH && python3 -m uvicorn voice_bridge:app --host 0.0.0.0 --port $PORT"


