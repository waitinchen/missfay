# ✅ 验证更新后的 .env 配置

## 验证时间
2026-01-03

## 验证项目

### 1. CARTESIA_API_KEY

**检查项**:
- Key 是否存在
- Key 长度是否正常（期望: 40-60+ 字符）
- Key 格式是否正确

**如果 Key 长度 < 30 字符**: 
- ⚠️ Key 可能仍然不完整
- 请前往 https://play.cartesia.ai 重新获取完整 Key

**如果 Key 长度 >= 40 字符**:
- ✅ Key 长度正常
- 可以继续测试

### 2. LONG_TERM_MEMORY_PATH

**检查项**:
- 配置项是否存在
- 文件路径是否正确
- 文件是否存在

**期望值**: `C:\Users\waiti\missfay\k\FAY024.md`

### 3. 其他配置

**GEMINI_API_KEY**: 应该已配置
**GEMINI_MODEL**: 可选，默认使用 `gemini-2.0-flash-exp`
**PROXY_PORT**: 可选，默认使用 `8001`

## 下一步操作

### 如果所有检查通过：

1. **关闭所有进程**:
   ```powershell
   .\kill_python_processes.ps1
   ```

2. **重新启动服务**:
   ```powershell
   .\start_voice_bridge.ps1
   ```

3. **验证修复**:
   - 访问 http://localhost:8000/
   - 检查对话框右上方的健康监控：`LLM: OK  TTS: OK`
   - 发送测试消息，确认不再出现 401 错误

### 如果 CARTESIA_API_KEY 仍然过短：

1. 访问 https://play.cartesia.ai
2. 登录账户
3. 进入 API Keys 设置
4. 复制完整的 API Key
5. 更新 `.env` 文件
6. 重新运行验证脚本

---

**验证脚本**: `check_env_complete.py`  
**状态**: 等待验证结果


